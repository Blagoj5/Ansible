pipeline {
    agent any

    stages {

        stage('Copying') {
            	steps {
			echo "Starting script"
		}

		steps {
                
			bash /tmp/autoscript.sh
            }
        }

}
}
