pipeline {
    agent any

    stages {

        stage('Copying') {
	
		steps {
			echo "Starting to copy"
		}
		

		steps {
                
			sh "bash /tmp/autoscript.sh"
                }
          }

	}
}
